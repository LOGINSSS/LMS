{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/LOGINSSS/LMS/main/docs/api-schemas/R.schema.json",
  "title": "统一响应体 R",
  "description": "后端所有接口的统一包装结构。code=1 表示成功，data 携带业务负载；失败时 code 为业务错误码，data 缺席（Java 侧 @JsonInclude(NON_NULL)）。前端 axios 封装（lms-web/src/api/request.js）在 code=1 时解包直接返回 data。",
  "type": "object",
  "required": ["code"],
  "properties": {
    "code": {
      "type": "integer",
      "description": "状态码：1 成功，其余为业务错误码（见各模块 ErrorInfo）"
    },
    "msg": {
      "type": ["string", "null"],
      "description": "提示信息"
    },
    "data": {
      "description": "业务负载，类型随接口而定（引用各模块 schema 的 $defs）"
    }
  },
  "additionalProperties": false
}
