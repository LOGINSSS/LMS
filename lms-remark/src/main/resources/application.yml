spring:
  application:
    name: lms-remark
  cloud:
    nacos:
      server-addr: localhost:8848
  config:
    import:
      - optional:nacos:lms-common.yaml?group=DEFAULT_GROUP&refreshEnabled=true
      - optional:nacos:lms-remark.yaml?group=DEFAULT_GROUP&refreshEnabled=true
