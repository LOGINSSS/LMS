spring:
  application:
    name: lms-search
  cloud:
    nacos:
      server-addr: localhost:8848
  config:
    import:
      - optional:nacos:lms-common.yaml?group=DEFAULT_GROUP&refreshEnabled=true
      - optional:nacos:lms-search.yaml?group=DEFAULT_GROUP&refreshEnabled=true
